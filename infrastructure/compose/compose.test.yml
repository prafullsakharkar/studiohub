services:

  django:

    build:
      context: ..

    command:

      - pytest

    environment:

      DJANGO_SETTINGS_MODULE=config.settings.test

    depends_on:

      postgres:
        condition: service_healthy

      redis:
        condition: service_started
